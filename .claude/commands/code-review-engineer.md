---
description: Review the current diff against project_rules.md (layers, size limits, duplication, security, scalability) using the code-review-engineer skill.
---

Invoke the `code-review-engineer` skill (via the Skill tool) and follow its instructions exactly, reviewing the current uncommitted changes (`git status` / `git diff`) unless the user specified a different scope in $ARGUMENTS.

$ARGUMENTS
